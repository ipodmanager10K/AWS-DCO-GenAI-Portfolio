# [교육용] SAMPLE_TOR_SW_01 스위치 Gi0/1 포트 통신 중단 사건 보고서

> [!NOTE]
> 본 보고서는 실제 AWS 인프라에서 발생한 장애가 아닙니다. 교육용 샘플 데이터를 분석하는 실습을 위해 작성한 모의 보고서입니다.

---

## 1. 보고서 제목
SAMPLE_TOR_SW_01 스위치 Gi0/1 포트 통신 중단(Link Down) 사건 분석 보고서

## 2. 작성 목적
- 교육용 샘플 로그 데이터를 바탕으로 가상의 DCO(Data Center Operations) 장애 분석 보고서를 작성하기 위함입니다.
- 네트워크 장비 장애 시 로그를 분석하고 원인을 추론하는 과정을 실습합니다.

## 3. 교육용 사건 개요
2026년 7월 3일 오전 3시 5분경, 네트워크 스위치 장비의 특정 포트(Gi0/1)에서 신호 왜곡 에러(CRC 에러)가 갑자기 늘어났습니다. 1분 뒤인 오전 3시 6분에는 서버와의 연결이 완전히 끊어졌습니다. 현장 조치팀이 케이블을 새것으로 교체하고 포트 연결 부위를 청소한 후 통신이 다시 정상으로 돌아왔습니다.

## 4. 관련 샘플 장비와 Ticket
- **장비명:** `SAMPLE_TOR_SW_01` (네트워크 스위치)
- **장비 IP 주소:** `192.0.2.1`
- **장애 포트 (인터페이스):** `Gi0/1`
- **관련 장애 티켓 ID:** `EDU-TKT-2026-0003`

## 5. 시간순 이벤트 (Time Timeline)

| 발생 시각 (Time) | 이벤트 종류 (Event) | 확인된 내용 |
| :--- | :--- | :--- |
| **03:05** | CRC error 증가 | 5분 동안 Gi0/1 포트의 CRC 에러 누적 수가 154로 증가했습니다. |
| **03:06** | Link Down | Gi0/1 포트의 상태가 비활성(DOWN) 상태로 바뀌며 통신이 중단되었습니다. |
| **03:07** | Ticket opened | 포트 통신 중단에 대해 장애 티켓 `EDU-TKT-2026-0003`이 생성되었습니다. |
| **03:12** | Ticket escalated | 현장 케이블 조치팀(Onsite Cabling Team)으로 티켓이 전달되었습니다. |
| **03:25** | Maintenance completed | LC-LC 멀티모드 패치 코드 케이블을 교체하고, 포트 커넥터를 청소했습니다. |
| **03:26** | Link Up | Gi0/1 포트 상태가 활성(UP) 상태로 변경되었습니다. |
| **03:30** | Normal heartbeat | 통신 에러가 0인 상태로 포트가 정상 작동하기 시작했습니다. |

## 6. 로그에서 확인된 사실
- 통신이 끊어지기 5분 전부터 Gi0/1 포트에서 전송 신호가 깨지는 현상(CRC 에러)이 154건 발생했습니다.
- 통신선(케이블)을 새로 바꾸고 먼지를 닦아내는 작업을 끝낸 직후에 통신 단절 문제가 해결되었습니다.
- 교체 작업 완료 이후에는 더 이상 해당 포트에서 통신 에러가 발생하지 않는 정상 상태가 유지되었습니다.

## 7. 가능한 원인
본 장애의 발생 원인으로 아래 세 가지 가능성을 추정하며, 특정 원인으로 확정하지 않습니다.

1. **광케이블 부품의 노후화 및 내부 손상**
   - 두 장비를 연결하는 광케이블(LC-LC 멀티모드) 선로 내부에 미세한 균열이나 손상이 있어 신호가 제대로 전달되지 않았을 수 있습니다.
2. **연결 커넥터 단자의 먼지 오염**
   - 케이블이 꽂히는 단자 부분에 미세한 먼지나 이물질이 끼어 전송 신호에 간섭을 일으켰을 수 있습니다.
3. **광학 모듈(SFP)의 미세한 접촉 불량**
   - 스위치 장비 자체의 광 신호 변환 모듈이 일시적으로 느슨하게 조립되어 접촉 상태가 나빠졌을 수 있습니다.

## 8. 현재 확인되지 않은 내용
- 장애가 일어나기 전에도 해당 포트에서 가끔씩 통신 에러가 발생했었는지에 대한 과거 이력
- 기존 케이블을 통해 들어오던 빛 신호의 세기(광학 수신 파워) 실측 데이터
- 케이블 반대편에 연결되어 있던 서버 쪽 네트워크 하드웨어의 결함 여부

## 9. 추가로 필요한 정보
1. 수거한 기존 불량 의심 광케이블에 대한 테스트 및 단면 먼지 정밀 검사 결과
2. 스위치 장비 광학 모듈(SFP)의 빛 신호 수신 감도를 주기적으로 수집한 모니터링 데이터
3. 장비가 설치된 방(랙) 내부의 먼지 상태 및 공조 필터 청소 주기 기록

## 10. Escalation 전달 메모
- **전달 대상:** 현장 케이블 조치팀 (Onsite Cabling Team)
- **내용:** SAMPLE_TOR_SW_01 스위치의 Gi0/1 포트에서 신호 이상 에러가 누적되며 통신이 정지되었습니다. 물리적인 케이블 교체와 커넥터 이물질 제거 작업을 통해 장애를 해결하고 통신을 복구시켰습니다.

## 11. 최종 요약
스위치 장비(`SAMPLE_TOR_SW_01`)의 포트(`Gi0/1`)에서 통신 에러가 급격하게 일어난 후 물리 링크가 중단되었습니다. 현장에서 통신 케이블 교체와 포트 클리닝 조치를 함께 진행하여 신속하게 서비스를 복구했습니다. 복구 이후 현재까지 에러 없이 안정적으로 작동 중입니다.

---

### 📂 분석에 활용한 참고 파일
- 로그 데이터 파일: [sample_dco_log.txt](file:///C:/AWS-DCO-GenAI-Portfolio/09_log_analysis_script/sample_dco_log.txt)
- 로그 통계 요약 보고서: [incident_summary.md](file:///C:/AWS-DCO-GenAI-Portfolio/09_log_analysis_script/incident_summary.md)
- 에러 원인 개별 분석서: [crc_linkdown_analysis.md](file:///C:/AWS-DCO-GenAI-Portfolio/10_incident_analysis/crc_linkdown_analysis.md)
